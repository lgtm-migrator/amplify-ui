import { ComponentStyles } from '../../../theme';
import { button } from '../../../primitives/Button/tokens';
import { defaultTheme } from '@aws-amplify/ui';

const { fieldcontrol, field } = defaultTheme.tokens.components;

export const components: ComponentStyles = {
  button,
  fieldcontrol,
  field
};
